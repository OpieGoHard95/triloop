// Jared Smith
// Ch 2
// Makes a triangle of hashtags
// Last work was done on Saturday
for (var hashtag = "#"; hashtag <= "#######"; hashtag = hashtag + "#")
	console.log(hashtag);
// Needed to alter somethng to test github, this will be deleted
